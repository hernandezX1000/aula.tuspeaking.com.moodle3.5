# Sesión 2026-07-10

> Bitácora de trabajo. Qué se hizo, qué se decidió, qué quedó abierto.
> Convención: un fichero por sesión, `YYYY-MM-DD.md`. Enlazar tickets del BACKLOG por su ID.

## Hecho

- **Autocorrector — writings subidos como fichero (AC-0).** El corrector solo leía `onlinetext` (texto pegado); los alumnos que subían .docx/.pdf/.odt/.txt/.rtf quedaban sin corregir. Añadido `extract_text_from_file` + `fetch_pending_file_writings` + `process_file_writings`. Desplegado en producción por el flujo dev→main→`deploy.sh`. **19 entregas corregidas.** Deps nuevas: python-docx, pypdf, odfpy, striprtf.
- **Ingesta Zoom verificada sana (ING-0).** El bug del null (`getZoomAPIParticipants`) ya estaba arreglado; se confirmó que corre el pipeline completo sin errores. Dos despistes: nadie la había relanzado desde las 16:44, y el log real está en ruta relativa (`admin/cli/logs/...`), no en `cron.log`.
- **Revisión de crons y notificaciones.** Todos los crons activos y logueando hoy; heartbeat cada hora ("OK - todo al día"). Detectado que el `mail` local entrega con retraso y pinta de spam; Moodle usa Gmail SMTP (llega instantáneo). → BACKLOG NOT-1/NOT-2.
- **Flujo de desarrollo establecido.** Clon local en Mac = clon del repo. Rama `dev` creada. Flujo: desarrollar en local sobre `dev` → probar en server con `--dry-run` → merge `dev→main` → `deploy.sh`. Helper de despliegue con backup en `tus-tools/autocorrect/deploy.sh`.
- **Organización de docs.** Creados `BACKLOG.md` (fuente única), `ROADMAP.md`, esta bitácora y `docs/README.md`. Consolidados los dos `TICKETS.md` solapados.

## Decidido

- **8 clases en "verificando" (26-jun→7-jul)** de Woermer/Olivia/Adrian/Daniela: **no tocar**, pueden tener ajuste manual en BD; empalman con decisiones por alumno. No es fallo de ingesta.
- **~10 entregas con fila de nota `grade=-1`** de tutores reales (Alvaro, Adiala, Eldina): **dejar a los tutores**, el bot no las toca (no ampliar el filtro a grade<0).
- **Notificaciones:** un digest 8:00 + 20:00 con todo el estado + alerta inmediata solo para lo crítico. Todo por Gmail SMTP.

## Abierto (ver BACKLOG)

- NOT-1/NOT-2: montar el digest 8/20h + `send_alert.py` (Gmail SMTP).
- SEC-1: rotar token GitHub (expuesto en chat).
- OPS-1/OPS-2: recuperaciones Linda Mohnssen y Adrian Barrena.
- AC-2 (edge-cases 41657/41728), AC-3 (endurecer JSON), ING-3 (log_path absoluto), NOT-3 (`bc`).
- OPS-3: decidir si pausar el cron del Samsung survey.
