# Sesión 2026-07-30 — Tickets de soporte del aula (Moodle 3.5 Docker / Hetzner LMS)

> Entorno: aula migrada → contenedor `moodle35-app` en `tuspeaking-lms` (46.225.232.27, `coreadmin`).
> BD: `moodle35-db` en `127.0.0.1:3307`, BD `aulatuspeaking35`. Scripts Python en `/home/coreadmin/scripts/`, `.env` en `/home/aulatuspeaking/.env`.

## 1. Autocorrector de quizzes — traducción inglés→español mal corregida (RESUELTO)

**Reporte:** Diego Sans (Hyatt, `diego.sans@hyatt.com`, curso EN B2, attempt 19132): las actividades de "traducir de inglés a español" no se validaban; el corrector exigía la respuesta en inglés.

**Causa:** `tus-tools/autocorrect/hansel_quiz_grader.py` asumía que toda traducción era Español→Inglés (prompt hardcode) → penalizaba con 0.0 las respuestas correctas en español. Además la detección de tipo miraba solo el texto de la pregunta (la instrucción "Translate to Spanish" está en el **nombre/intro del quiz**), y la config de BD había quedado con la ruta de socket vieja.

**Fix (commit `b398aac` en `main`):**
- Detección de tipo con **nombre + intro del quiz** (no solo la pregunta).
- Prompt de traducción **agnóstico de dirección** (`translation_system()`): juzga fidelidad, nunca penaliza por idioma, marca 0 si repite el mismo idioma que el origen.
- `DB_CONFIG` → **`127.0.0.1:3307`** (post-migración la BD está en Docker; pymysql con `localhost` iba por socket → error 1698).
- Modo **`--attempt <id>`** para re-corregir intentos ya calificados (`save_quiz_grade` añade un paso nuevo, no machaca).

**Remediación:** re-corregidos los **15 intentos afectados de 2026** (filtrados por feedback "requires English"; el universo de 646 traducciones NO eran víctimas). Notas ahora por mérito (mayoría 3,5–4,5/5). **Cron del grader REACTIVADO** (`0 */4 * * *`, estaba comentado). Fleco: 1 pregunta de Diego (viuda/viudo, 19147) con 0.0 por fallo puntual del modelo → revisar a mano.

## 2. José Tomás Morán (`jose.moran@e2ycommerce.com`, user 5334) — clase no contabilizada (RESUELTO)

**Reporte:** clase del 29-jul 16:00 con Candice Lazenby (sustituyó a una cancelada de Rj Reddy) no aparecía en "Mis clases".

**Causa:** la clase de Candice **no tenía fila** en `mdl_i3code_acuityZoom` — solo la de Rj Reddy (cancelada). Al reservar Candice en el **mismo slot** (mismo `studentid`+`acuity_datetime`+`courseid`), el INSERT chocaba con la clave única `idx_unique_clase` (error 1062) → la clase nueva no se registraba.

**Fix (manual):** UPDATE de la fila de Rj Reddy (110711) → Candice (`teacherid=1684`), `acuity_canceled=0`, `zoom_clasecompletada=1`, `manual_override=1`. Completadas del curso 4061: 2→3.

**Fleco de código (PENDIENTE):** que el INSERT sea **UPSERT** (`ON DUPLICATE KEY UPDATE`) en `admin/cli/i3code_download_zoomdata.php` (en el repo) y en `modifyAndCreateAcuity.php` (raíz web del server, fuera del repo).

## 3. Webhooks de Acuity — config leftover de la migración (config corregida por Hansel)

**Hallazgo:** en Acuity, los webhooks dedicados **"New Appointment Scheduled"** y **"Appointment Rescheduled"** apuntaban a `https://api.tuspeaking.com/api/v1/webhooks/acuity` (la **plataforma NUEVA**, no el aula) → 500 → Acuity los **deshabilitó**. Los correctos: **Canceled → `cancelAcuity.php`** y **catch-all "Scheduled or Updated" → `modifyAndCreateAcuity.php`** (ambos → aula, funcionan).

**Verificado:** la **reprogramación automática funciona sin el webhook dedicado** (lo hacen el catch-all + el cron `i3code_download_zoomdata.php`): 18/20 reprogramaciones recientes procesadas solas con cambio de hora real.

**Fix:** vaciar los 2 campos rotos en Acuity (no apuntarlos a `api.tuspeaking.com`).

## 4. Rafael Herrador (`rafael.herrador@hyatt.com`, user 9965) — cancelar 2 clases de hoy (RESUELTO)

Pidió cancelar sus clases del 30-jul 14:00 y 14:30 (Amber Gendron) por viaje; el sistema no le dejaba (ventana de cancelación). Marcadas `acuity_canceled=1` + `manual_override=1` (filas 111018/111019) para que no cuenten como ausencia. Cancelación real en Acuity la hizo Hansel (acuityid 1745620632 / 1745620633).

## 5. GDES Francés — completion de páginas (Luis Miguel, Noelia) — VERIFICADO, sin bug

**Contexto:** el caso previo de **Nicolás Bilanin** (5822) SÍ tenía bug: las páginas teóricas de Gramática (`page`, `completionview=1`) no marcaban finalización al verlas → se arregló (completionview en la plantilla + backfill; 11%→31%). La plantilla ya quedó bien en los cursos GDES francés (3242, 3199/3200/3201).

**Reporte (Daniela, GDES):** Luis Miguel Alonso Porro (5823) y Noelia Juan (5733) "el mismo problema".

**Verificación:** ambos con **0 páginas vistas-sin-marcar** (log completo desde 2018 → fiable). Luis Miguel (curso 3242): 26/80 = **32%**; Noelia (curso 3200): 51/80 = **64%**. Su completion es **correcta**; el % refleja su avance real, no un bug. **Sin cambios en BD.** GDES confirma que se ve bien.

**Nota:** Noelia en el aula es "Noelia Juan" (no "Alonso"), curso **3200** ("2026.1 - GDES - Frances B2 - Noelia Juan"), no 3242.
